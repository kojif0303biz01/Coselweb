# スパイク（一時的大量発注）対応戦略

## 🔍 スパイク分析結果サマリー

### 検出されたスパイクの実態

| カテゴリ | スパイク数 | 平均倍率 | 最大倍率 | 連続パターン |
|---------|----------|---------|---------|------------|
| **PCA** | 96個 | **7.65x** | **62.69x** | 63パターン |
| **PBA** | 48個 | **5.17x** | **14.42x** | 33パターン |
| **TUHS** | 49個 | **4.38x** | **11.78x** | 40パターン |

### 重要な発見

#### 1. **極端なスパイクの存在**

**PCA1000F-48-T の事例:**
- 2021年5月: 586台（通常9台の **62.7倍**）
- 2021年1月: 338台（通常9台の **36.2倍**）
- 2022年4月: 279台（通常9台の **29.8倍**）

**PBA300F-24-F4 の事例:**
- 2022年7月: 4,003台（通常277台の **14.4倍**）
- 2022年4月: 3,700台（通常277台の **13.3倍**）

#### 2. **連続した高販売パターン**

**PCA300F-5-T:**
- 2022年6月～2023年5月: 4か月連続で通常の **88.4倍**
- キャンペーンや大口契約の可能性

**PBA300F-24-F4:**
- 2022年3月～8月: 6か月連続で通常の **51.2倍**
- 特定顧客への大量供給と推測

#### 3. **カテゴリ全体でもスパイク発生**

- PCA: 2024年7月に通常の2.22倍（3,491台）
- PBA: 2022年4月に通常の1.74倍（93,465台）
- TUHS: 2022年1月に通常の2.02倍（121,241台）

---

## 🎯 スパイクを考慮した予測戦略

### 基本方針: **2層モデリングアプローチ**

ご指摘の通り、**ベース需要**と**一時的大量発注**を分けて扱うことで予測精度を大幅に向上できます。

```
┌────────────────────────────────────────────────────┐
│        総予測 = ベース需要 + スパイク調整            │
└────────────────────────────────────────────────────┘
                    ↓                    ↓
         ┌──────────────────┐  ┌──────────────────┐
         │  ベース需要予測   │  │ スパイク考慮      │
         │  (通常の販売)    │  │ (一時的大量発注)  │
         ├──────────────────┤  ├──────────────────┤
         │ ・トレンド       │  │ ・異常値検出      │
         │ ・季節性         │  │ ・パターン学習    │
         │ ・製品ライフサイクル│  │ ・確率的予測    │
         └──────────────────┘  └──────────────────┘
```

---

## 📋 実装アプローチ

### アプローチ1: 異常値除外 + ロバストモデリング（推奨★★★★★）

#### ステップ1: スパイクの検出と分離

```python
# 1. 統計的手法でスパイクを検出
- IQR法: 四分位範囲の2倍を超える値
- Zスコア法: 標準偏差の2.5倍を超える値

# 2. スパイクフラグを作成
df['is_spike'] = 異常値検出結果

# 3. ベースデータとスパイクデータに分離
base_data = df[~df['is_spike']]  # 通常需要
spike_data = df[df['is_spike']]   # 一時的大量発注
```

#### ステップ2: ベース需要の予測

```python
# スパイクを除外したデータで予測モデルを学習
model.fit(base_data)

# 安定した予測が可能
base_forecast = model.predict(future)
```

#### ステップ3: スパイク調整（3つのオプション）

**オプションA: 保守的アプローチ（デフォルト）**
- ベース需要のみで予測
- スパイクは予測不可能として無視
- 安全側の予測

**オプションB: スパイク確率的予測**
- 過去のスパイク発生パターンを学習
- 「3か月に1回、5倍のスパイク」などを確率的に考慮
- 期待値ベースの予測

**オプションC: 外部情報活用（理想）**
- 営業情報（大口契約の予定）
- キャンペーン計画
- 外部要因（市場トレンド）

#### メリット
- 予測が安定する
- 異常値に振り回されない
- ベースラインが正確になる

#### デメリット
- スパイクを予測しないため、実際より低めの予測

---

### アプローチ2: Prophetのロバスト設定（推奨★★★★☆）

Prophetには**異常値に対して頑健な機能**が標準装備されています。

```python
from fbprophet import Prophet

# ロバスト設定
model = Prophet(
    # 異常値の影響を軽減
    changepoint_prior_scale=0.05,  # トレンド変化の柔軟性（低めに設定）

    # 季節性の設定
    yearly_seasonality=True,
    weekly_seasonality=False,
    daily_seasonality=False,

    # 異常値の検出と処理
    mcmc_samples=0,  # ベイズ推定（オプション）
)

# スパイクを「holidays」として登録
spike_dates = spike_data[['年月']].rename(columns={'年月': 'ds'})
spike_dates['holiday'] = '大量発注'
model.add_holidays(spike_dates)

# 学習
model.fit(train_data)
```

#### メリット
- 実装が簡単
- 自動的に異常値を考慮
- トレンド変化点の自動検出

---

### アプローチ3: 特徴量エンジニアリング（LightGBM）（推奨★★★★★）

機械学習モデルでは、**スパイク関連の特徴量**を明示的に追加します。

```python
# 特徴量の作成
df['前月スパイク'] = df['is_spike'].shift(1)
df['3か月以内スパイク'] = df['is_spike'].rolling(3).sum()
df['前回スパイクからの経過月数'] = ...
df['同製品の過去スパイク回数'] = ...
df['スパイク月の平均台数'] = ...

# LightGBMで学習
import lightgbm as lgb

# スパイク含めた全データで学習
model = lgb.LGBMRegressor(
    objective='regression',
    metric='rmse',
    # ロバスト損失関数
    boost_from_average=True,
)

model.fit(X_train, y_train)
```

#### 追加する特徴量

1. **スパイク関連**
   - 前月がスパイクか（0/1）
   - 過去3/6/12か月のスパイク回数
   - 前回スパイクからの経過月数
   - 同製品の歴史的スパイク頻度

2. **ベース需要関連**
   - スパイク除外後の移動平均
   - スパイク除外後のトレンド
   - 通常時の季節係数

3. **製品特性**
   - 製品カテゴリ
   - 製品の販売開始月数
   - 製品の平均販売台数

#### メリット
- 最も高精度が期待できる
- スパイクのパターンも学習可能
- 柔軟性が高い

---

### アプローチ4: 2段階予測モデル（高度、推奨★★★☆☆）

```python
# ステップ1: スパイク発生確率の予測
spike_classifier = LGBMClassifier()
spike_classifier.fit(X_train, is_spike_train)
spike_prob = spike_classifier.predict_proba(X_test)

# ステップ2: ベース需要の予測
base_model = Prophet()
base_model.fit(base_train_data)
base_forecast = base_model.predict(future)

# ステップ3: スパイク時の販売量予測
spike_model = LGBMRegressor()
spike_model.fit(X_spike_train, y_spike_train)
spike_forecast = spike_model.predict(X_test)

# 統合予測
final_forecast = (
    base_forecast * (1 - spike_prob) +
    spike_forecast * spike_prob
)
```

#### メリット
- 理論的に最も正確
- スパイクとベースを明確に分離

#### デメリット
- 実装が複雑
- 2つのモデルの調整が必要

---

## 🔧 推奨する実装計画（改訂版）

### フェーズ1: ベースライン + スパイク分析（1-2日）

```
✅ 完了
- 移動平均
- 前年同月比
- スパイク検出
```

### フェーズ2: Prophet（ロバスト設定）（2-3日）

```
□ スパイク除外版Prophetモデル
  - 通常需要のみで学習
  - 安定したベースライン予測

□ スパイク考慮版Prophetモデル
  - holidays機能で大量発注を登録
  - changepoint_prior_scaleを調整
```

### フェーズ3: LightGBM + 特徴量エンジニアリング（3-5日）

```
□ スパイク関連特徴量の追加
  - 前月スパイクフラグ
  - 過去スパイク回数
  - ベース需要の移動平均

□ ロバスト学習
  - 全データで学習
  - スパイクパターンも学習

□ 予測時の調整
  - スパイク確率の考慮
```

### フェーズ4: アンサンブル + 評価（1-2日）

```
□ 複数モデルの組み合わせ
  - Prophet（ベース需要）: 40%
  - LightGBM（全パターン）: 60%

□ スパイク考慮の評価
  - スパイク除外時の精度
  - スパイク含む時の精度
  - ビジネス観点での評価
```

---

## 📊 評価指標の拡張

### 従来の指標

- RMSE, MAPE, MAE（全体）

### スパイク考慮の追加指標

1. **ベース需要の精度**
   - スパイク月を除外した精度
   - 「通常時にどれだけ正確か」

2. **スパイク検出精度**
   - スパイク月を正しく識別できたか
   - 精度 (Precision) / 再現率 (Recall)

3. **ビジネス影響**
   - 過小予測: 在庫切れリスク
   - 過大予測: 在庫過剰リスク
   - 非対称な評価

---

## 🎯 最終推奨アプローチ

ご指摘を踏まえ、以下の**ハイブリッド戦略**を推奨します：

### 主軸: 3モデル並行開発

1. **Prophet（スパイク除外版）** ⭐⭐⭐⭐⭐
   - ベース需要の安定予測
   - 解釈性が高い
   - スパイクを異常値として自動処理

2. **Prophet（スパイク考慮版）** ⭐⭐⭐⭐☆
   - holidays機能で既知スパイクを登録
   - ロバストパラメータ設定

3. **LightGBM（スパイク特徴量追加）** ⭐⭐⭐⭐⭐
   - スパイクパターンを特徴量として学習
   - 最高精度が期待できる
   - ベース + スパイクの両方を予測

### 最終予測の作成

```python
# ベース需要予測（保守的）
base = prophet_robust.predict()

# パターン学習予測（積極的）
pattern = lightgbm_model.predict()

# 加重平均（ビジネス要件に応じて調整）
final = base * 0.4 + pattern * 0.6

# または、信頼区間を提供
prediction = {
    '保守的': base,
    '標準': final,
    '積極的': pattern
}
```

---

## 💡 ビジネス的な考慮事項

### スパイクの予測可能性

1. **予測可能なスパイク**
   - 定期的なキャンペーン
   - 既知の大口契約
   - 季節的なイベント
   → **外部情報として追加**

2. **予測不可能なスパイク**
   - 突発的な大量発注
   - 新規顧客の獲得
   - 競合の影響
   → **確率的に考慮** または **保守的に無視**

### 推奨される運用

1. **通常予測**: スパイク除外版Prophet
   - 安定した計画が可能
   - 在庫管理のベース

2. **上限予測**: LightGBM
   - スパイク可能性を含む
   - リスク管理用

3. **外部情報の統合**
   - 営業からの情報
   - 「来月大口案件がある」→ 手動調整

---

## ✅ まとめ

ご指摘いただいた**一時的大量発注とベース需要の分離**は、予測精度向上の**最重要ポイント**です。

### 実装する機能

✅ **スパイク検出**
- IQR法とZスコア法の組み合わせ
- 製品別・月別の異常値識別

✅ **2層モデリング**
- ベース需要モデル（スパイク除外）
- スパイク考慮モデル（特徴量追加）

✅ **複数シナリオ予測**
- 保守的予測（スパイクなし）
- 標準予測（確率的スパイク）
- 積極的予測（パターン学習）

✅ **評価の拡張**
- ベース需要精度
- スパイク検出精度
- ビジネス影響評価

これにより、**通常時の安定予測**と**スパイク時の柔軟対応**の両立が可能になります！

次のステップでこれを実装していきます。
